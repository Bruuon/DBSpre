# 🚀 CentOS 扩容记录

这个不用放进ppt，写进报告里遇到的问题&收获部分就行

遇到的问题是：为了SQL优化，索引建的有点多（反正这个表也不需要DML操作），虚拟机最初只给了20G的磁盘容量，快不够用了，需要扩容。
下面是解决方法。
---
### 1. 物理扩容
删除所有快照，在vmware里给磁盘扩容至40G。
### 2. 分区重划
这一步非常底层，主要是把新增的空白空间划分为一个新的物理分区（比如 `sda3`）。

    fdisk /dev/sda

进入 `fdisk` 交互模式后，依次输入
* 输入 `n` (新建分区 new)
* 输入 `p` (创建主分区 primary)
* 输入 `3` (分区号，假设你原来有 sda1 和 sda2，这里就是 3)
* 连续按 **两次回车** (默认使用所有新增的柱面/扇区空间)
* 输入 `t` (修改分区类型 type)
* 输入 `3` (选中刚才建的第3个分区)
* 输入 `8e` (将类型改为 Linux LVM，这一步最关键)
* 输入 `w` (保存并退出 write)


### 3. 逻辑变现 (LVM 抽象层)
接下来把刚才建好的物理分区 `sda3` 加入到系统的逻辑卷组里，并分配给根目录 `/`。

创建物理卷 (PV)：

    pvcreate /dev/sda3

将新物理卷加入到系统的卷组 (VG) 中 (注意：`centos` 是默认卷组名，可通过 `vgdisplay` 查看)：

    vgextend centos /dev/sda3

将卷组中所有剩余的空闲空间，全部分配给根目录所在的逻辑卷 (LV)：

    lvextend -l +100%FREE /dev/mapper/centos-root

### 4. 挂载
此时逻辑卷变大了，但文件系统还不知道。CentOS 7 默认使用 XFS 文件系统，使用以下命令刷新：

    xfs_growfs /dev/mapper/centos-root


---

写进报告里的收获：接触挂载概念，学习了linux分区操作，对linux与windows的文件组织区别有了更加深入的认识。